
    OFA_APP_LABEL="OfaAdmin"

    BUNDLE_NAME_FORMAT="$OFA_BUNDLE_NAME_FORMAT"
    BUNDLE_DIR="$OFA_BUNDLE_DIR"
    BUNDLE_DIR_PROD="$OFA_BUNDLE_DIR_PROD"
    STAGING_DIR="$OFA_STAGING_DIR"
    STAGING_DIR_PROD="$OFA_STAGING_DIR_PROD"
    MASTER_HOST="$OFA_MASTER_HOST"
    MASTER_OWN="$OFA_MASTER_OWN"
    SCOPE=DEV

    OFA_MAIL_RCP_BAD="mo nail" 

    return 0
